---
- name: install and start webserver
  hosts: localhost
  tasks:
    - name: install packages
      yum:
        name: httpd
        state: latest
    - name: start service
      service:
        name: httpd
        state: started
        enabled: yes
